/**
 * Shared typography classes for consistent typeface usage across the app.
 * Use these constants instead of inline class strings for labels, headings, and body text.
 */

/** Form/field labels above inputs (16px, bold, uppercase, Arquitecta, muted). */
export const FIELD_LABEL =
  'text-base font-bold uppercase tracking-wider text-muted-text font-arquitecta';

/** Body/input value text (16px, Open Sans, charcoal). */
export const BODY_TEXT = 'text-base text-charcoal font-opensans';

/** Section heading (e.g. "Guest information") — 2xl, bold, uppercase, Arquitecta. */
export const SECTION_HEADING =
  'text-2xl font-bold uppercase tracking-wider font-arquitecta';

/** Page title (e.g. event/ticket flow title). */
export const PAGE_TITLE =
  'text-4xl md:text-5xl font-bold uppercase tracking-wider mb-6 font-arquitecta';

/** Card/sidebar section title (e.g. "Date", "Order Summary" label row). */
export const CARD_LABEL =
  'text-base font-bold uppercase tracking-wider text-muted-text font-arquitecta';
